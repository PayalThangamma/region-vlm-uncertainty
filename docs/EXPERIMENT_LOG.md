## Run 001 - Created Raw ROHE Dataset

**Date:** 2026-06-29  
**Script:** `code/01_create_rohe_dataset.py`

### Goal

Create a raw ROHE-style dataset from COCO by selecting medium-sized object instances and generating segmentation masks.

### Output

```text
data/rohe_raw/
```

### Result

```text
Successfully generated 636 candidate samples.
```

---

## Run 002 - Prepare LaMa Input

**Date:** 2026-06-29  
**Script:** `code/02_prepare_lama_input.py`

### Goal

Convert the raw ROHE dataset into the input format required by LaMa.

### Output

```text
lama_input/
```

### Result

```text
Successfully prepared 636 samples for LaMa.
```

---

## Run 003 - LaMa Inpainting

**Date:** 2026-06-29  
**Tool:** LaMa  
**Environment:** `lama39`

### Goal

Generate object-removed images from the raw ROHE dataset using the LaMa inpainting model.

### Output

```text
lama_output/
```

### Result

```text
LaMa inpainting completed successfully.
```

---

## Run 004 - Copy LaMa Outputs

**Date:** 2026-06-29  
**Script:** `code/03_copy_lama_output.py`

### Goal

Copy the inpainted images generated by LaMa back into each sample directory as `removed.png`.

### Output

```text
data/rohe_raw/sample_*/removed.png
```

### Result

```text
Successfully copied 636 LaMa outputs into the corresponding sample directories.
```

---

## Run 005 - Region Map Generation

**Date:** 2026-06-30  
**Script:** `code/04_build_region_maps.py`

### Goal

Generate semantic region maps for every raw ROHE sample.

### Parameters

```text
IMAGE_SIZE = 336
PATCH_SIZE = 14
GRID_SIZE = 24
EXPECTED_TOKENS = 576
DILATION_SIZE = 35
OVERLAP_THRESHOLD = 0.25
```

### Output

```text
outputs/region_maps_rohe/
```

### Result

```text
Samples found: 636
Successful samples: 636
Skipped samples: 0
total = 576 for every successful sample
```

### Raw Log

```text
logs/run_04_build_region_maps.txt
```

---

## Run 006 - Quality Filtering for Region Maps

**Date:** 2026-06-30  
**Script:** `code/05_quality_filter.py`

### Goal

Filter the raw ROHE samples using token-region statistics.

### Filtering Criteria

```text
total == 576
background >= 250
20 <= removed <= 150
30 <= context <= 200
```

### Output

```text
outputs/rohe_quality.csv
```

### Result

```text
Samples checked: 636
Good samples: 522 / 636
Rejected samples: 114 / 636
```

### Raw Log

```text
logs/run_05_quality_filter.txt
```

---

## Run 007 - Create Final Common Dataset

**Date:** 2026-06-30  
**Script:** `code/06_create_final_dataset.py`

### Goal

Create the final common ROHE dataset by copying only quality-approved samples into a separate final dataset directory.

### Output

```text
data/rohe_final/
outputs/region_maps_final/
outputs/final_dataset_manifest.json
```

### Result

```text
Good samples found in quality CSV: 522
Final common dataset created.
Final samples: 522
```

### Raw Log

```text
logs/run_06_create_final_dataset.txt
```

---

## Run 008 - Prepare HPC Inputs

**Date:** 2026-06-30  
**Script:** `code/07_prepare_hpc_inputs.py`

### Goal

Prepare the final common ROHE dataset for HPC-based LLaVA and epistemic uncertainty experiments.

### Output

```text
outputs/hpc_inputs/
```

Generated folders:

```text
outputs/hpc_inputs/original_images/
outputs/hpc_inputs/removed_images/
outputs/hpc_inputs/removed_images_jpg/
outputs/hpc_inputs/token_regions/
```

Generated files:

```text
outputs/hpc_inputs/questions_original.jsonl
outputs/hpc_inputs/questions_removed.jsonl
outputs/hpc_inputs/questions_removed_jpg.jsonl
outputs/hpc_inputs/hpc_manifest.json
```

### Result

```text
Prepared samples: 522
Skipped samples: 0
```

### Raw Log

```text
logs/run_07_prepare_hpc_inputs.txt
```

---

## Run 009 - Upload and Verify HPC Inputs on Cluster

**Date:** 2026-06-30

### Goal

Move the prepared HPC input package from the local machine to the cluster and verify file counts.

### HPC Location

```text
/nethome/ptasathish/projects/region-vlm-uncertainty/hpc_inputs/
```

### Verification

```text
original_images: 522
removed_images: 522
removed_images_jpg: 522
token_regions: 522
questions_original.jsonl: 522
questions_removed.jsonl: 522
questions_removed_jpg.jsonl: 522
```

### Issue Encountered

The `token_regions/` folders initially had permission errors.

### Fix

```bash
chmod -R u+rwX hpc_inputs
```

### Result

```text
HPC input files uploaded and verified.
```

---

## Run 010 - Upload and Verify Patched Epistemic Code

**Date:** 2026-06-30  
**Location:** HPC  
**Log:** `logs/run_10_upload_verify_epistemic_code.txt`

### Goal

Upload the locally patched Epistemic repository to HPC and verify that ROHE-specific code changes are present.

### Verification

`attack.py` contained:

```text
rohe dataset branch
extension-safe output naming using os.path.splitext
rohe support in EVA and CLIP parser dataset choices
```

`eval_caption.py` contained:

```text
--token_region_root
dataset_name == rohe
sample_id support
has_cls token handling
region_mask_mode support
```

### Result

```text
Patched Epistemic code uploaded and verified on HPC.
```

---

## Run 011 - Prepare Smoke Test Inputs

**Date:** 2026-06-30  
**Location:** HPC  
**Log:** `logs/run_11_prepare_smoke_inputs.txt`

### Goal

Create a 3-sample smoke-test subset from the full ROHE HPC inputs.

### Smoke Samples

```text
sample_000062
sample_000068
sample_000069
```

### Verification

```text
removed_images_jpg: 3
removed_images: 3
original_images: 3
token_regions: 3
questions_original.jsonl: 3
questions_removed.jsonl: 3
questions_removed_jpg.jsonl: 3
```

### Result

```text
Smoke-test inputs prepared successfully.
```

---

## Run 012C - Attack Smoke Test Completed Successfully

**Date:** 2026-07-01  
**Location:** HPC GPU node

### Log Files

```text
logs/run_12c_attack_smoke.out
logs/run_12c_attack_smoke.err
logs/run_12c_attack_smoke.condor.log
```

### Goal

Run a small 3-sample smoke test of the patched Epistemic `attack.py` pipeline on ROHE using the CLIP attack branch.

### Configuration

```text
Script: Epistemic/baselines/attack.py
Mode: clip
Model: openai/clip-vit-large-patch14-336
Dataset: rohe
Epsilon: 3
Alpha: 1
Steps: 2
```

### Environment Verification

```text
torch 2.6.0+cu118 cuda True
transformers ok
cv2 ok
natsort ok
```

### Output

```text
outputs/smoke_attack_removed/sample_000062.png
outputs/smoke_attack_removed/sample_000068.png
outputs/smoke_attack_removed/sample_000069.png
```

### Result

```text
Status: Attack smoke test completed.
```

---

## Run 013 - Full Removed-Image Adversarial Attack

**Date:** 2026-07-01  
**Location:** HPC GPU node

### Log Files

```text
logs/run_13_attack_full_removed.out
logs/run_13_attack_full_removed.err
logs/run_13_attack_full_removed.condor.log
```

### Goal

Generate adversarial versions of all final ROHE removed images using the patched Epistemic CLIP attack pipeline.

### Input

```text
hpc_inputs/removed_images_jpg/
```

Input count:

```text
522
```

### Configuration

```text
Script: Epistemic/baselines/attack.py
Mode: clip
Model: openai/clip-vit-large-patch14-336
Dataset: rohe
Epsilon: 3
Alpha: 1
Steps: 200
```

### Output

```text
outputs/attack_removed_full/
```

Output count:

```text
522
```

### Runtime

```text
1:50:53
```

### Result

```text
CLIP Attack: 100%|██████████| 522/522 [1:50:53<00:00, 12.75s/it]
Status: Full removed-image adversarial attack completed.
```

---

## Run 014 - eval_caption.py Smoke Test

**Date:** Thu Jul 2 21:57:17 CEST 2026  
**Host:** tony-2.coli.uni-saarland.de  
**Status:** Completed successfully

### Environment

```text
Python 3.9.25
torch 2.6.0+cu118
CUDA available: True
transformers 4.57.6
```

### Purpose

Smoke-test patched `eval_caption.py` on 3 ROHE removed-image samples before launching full LLaVA evaluation jobs.

### Configuration

```text
model: llava-1.5-7b
decoder: greedy
dataset_name: rohe
image_folder: ../../hpc_inputs_smoke/removed_images_jpg
attack_image_folder: ../../outputs/smoke_attack_removed
output_dir: ../../outputs/smoke_eval_removed_all
num_samples: 3
max_new_tokens: 64
use_ours: enabled
region_mask_mode: all
token_region_root: ../../hpc_inputs_smoke/token_regions
```

### Outputs

```text
outputs/smoke_eval_removed_all/captions.jsonl
outputs/smoke_eval_removed_all/config.json
outputs/smoke_eval_removed_all/region_uncertainty/sample_000062.json
outputs/smoke_eval_removed_all/region_uncertainty/sample_000068.json
outputs/smoke_eval_removed_all/region_uncertainty/sample_000069.json
```

### Result

```text
3 / 3 samples completed
```

### Observed Captions

```text
sample_000062, target bottle: answer = no, correct after object removal
sample_000068, target car: answer = yes, hallucination
sample_000069, target bus: answer = yes, hallucination
```

### Region Uncertainty Sanity Check

For `sample_000062`:

```text
total patch tokens: 576
keep mask tokens including CLS: 577
uncertain patch tokens: 86
removed-region uncertainty density: 0.5347
context uncertainty density: 0.0244
background uncertainty density: 0.0205
```

### Conclusion

Run 014 confirmed that ROHE JSONL loading, removed-image loading, adversarial image loading, token-region loading, region-wise uncertain-token masking, LLaVA generation, captions writing, and region uncertainty writing all work on the smoke subset.

---

## Run 015A - Full Removed-Image Evaluation with Global Uncertain-Token Masking

**Status:** Completed successfully  
**Date:** Thu Jul 2 23:38:03 CEST 2026  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run full LLaVA evaluation on all 522 ROHE removed-image samples using global uncertain-token masking with `region_mask_mode=all`.

### Configuration

```text
model: llava-1.5-7b
decoder: greedy
dataset_name: rohe
num_samples: 522
max_new_tokens: 64
use_ours: enabled
region_mask_mode: all
token_region_root: ../../hpc_inputs/token_regions
```

### Outputs

```text
outputs/eval_removed_all/captions.jsonl
outputs/eval_removed_all/config.json
outputs/eval_removed_all/region_uncertainty/*.json
```

### Result

```text
522 / 522 samples completed
522 captions written
522 region_uncertainty JSON files written
runtime: approximately 39.7 minutes
```

### Conclusion

Full removed-image evaluation with global uncertain-token masking completed successfully.

---

## Run 015B - Full Removed-Image Evaluation Without Masking

**Status:** Completed successfully  
**Date:** Fri Jul 3 00:43:59 CEST 2026  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run the full no-masking baseline on all 522 ROHE removed-image samples.

This baseline is required to measure whether global and region-wise uncertain-token masking reduce object hallucination.

### Configuration

```text
Model: llava-1.5-7b
Decoder: greedy
Dataset name: rohe
Number of samples: 522
Max new tokens: 64
use_ours: enabled
region_mask_mode: none
```

### Output

```text
outputs/eval_removed_none/captions.jsonl
outputs/eval_removed_none/config.json
outputs/eval_removed_none/region_uncertainty/
```

### Output Verification

```text
522 outputs/eval_removed_none/captions.jsonl
522 region_uncertainty JSON files
```

### Result

```text
100%|██████████| 522/522 [39:57<00:00, 4.59s/it]
```

Runtime:

```text
greedy 2397.920686006546 seconds
```

Approximately:

```text
39.97 minutes
```

Final status:

```text
Status: Run 015B full eval removed none completed.
Fri Jul 3 00:43:59 CEST 2026
```

### Masking Verification

```text
Region mode: none
suppressed_patch: 0
patch_total: 576
keep_mask_total: 577
```

### Raw Logs

```text
logs/run_15b_eval_removed_none.out
logs/run_15b_eval_removed_none.err
logs/run_15b_eval_removed_none.condor.log
```

### Non-fatal Warnings

The `.err` log contained only non-fatal warnings:

```text
TRANSFORMERS_CACHE is deprecated and will be removed in v5 of Transformers.
Importing from timm.models.hub is deprecated.
Importing from timm.models.layers is deprecated.
Importing from timm.models.registry is deprecated.
torch_dtype is deprecated.
You are using a model of type llava to instantiate a model of type llava-1.5.
torch.cuda.amp.autocast is deprecated.
The generation flag top_p may be ignored.
```

### Conclusion

Run 015B successfully generated the no-masking baseline for all 522 removed-image samples.

---

## Run 015C - Full Removed-Image Evaluation with Removed-Region Masking

**Status:** Completed successfully  
**Date:** Fri Jul 3 01:51:30 CEST 2026  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run full LLaVA evaluation on all 522 ROHE removed-image samples while suppressing epistemically uncertain visual tokens only in the removed-object region.

This run tests whether hallucination is causally linked to uncertain visual tokens located in the region where the target object originally existed before object removal.

### Configuration

```text
Model: llava-1.5-7b
Decoder: greedy
Dataset name: rohe
Number of samples: 522
Max new tokens: 64
use_ours: enabled
region_mask_mode: removed
```

### Output

```text
outputs/eval_removed_removed/captions.jsonl
outputs/eval_removed_removed/config.json
outputs/eval_removed_removed/region_uncertainty/
```

### Output Verification

```text
522 outputs/eval_removed_removed/captions.jsonl
522 region_uncertainty JSON files
```

### Result

```text
100%|██████████| 522/522 [40:44<00:00, 4.68s/it]
```

Runtime:

```text
greedy 2444.449285030365 seconds
```

Approximately:

```text
40.74 minutes
```

### Masking Verification

The logs confirm that removed-region masking was active:

```text
Region mode: removed
patch_total: 576
keep_mask_total: 577
```

Several samples had non-zero removed-region suppression:

```text
Sample: sample_000571 Region mode: removed suppressed_patch: 10 patch_total: 576 keep_mask_total: 577
Sample: sample_000573 Region mode: removed suppressed_patch: 13 patch_total: 576 keep_mask_total: 577
Sample: sample_000579 Region mode: removed suppressed_patch: 15 patch_total: 576 keep_mask_total: 577
Sample: sample_000580 Region mode: removed suppressed_patch: 11 patch_total: 576 keep_mask_total: 577
Sample: sample_000599 Region mode: removed suppressed_patch: 23 patch_total: 576 keep_mask_total: 577
```

Some samples had:

```text
suppressed_patch: 0
```

This is expected. It means no uncertain patch tokens fell inside the removed-object region for those samples.

### Minor Logging Note

The final status line printed:

```text
Status: Run 015C full eval removed none completed.
```

This is only a script text-label typo. The actual run is correct because the output folder and per-sample logs confirm:

```text
outputs/eval_removed_removed/
Region mode: removed
```

### Raw Logs

```text
logs/run_15c_eval_removed_removed.out
logs/run_15c_eval_removed_removed.err
logs/run_15c_eval_removed_removed.condor.log
```

### Non-fatal Warnings

The `.err` log contained only non-fatal warnings:

```text
TRANSFORMERS_CACHE is deprecated and will be removed in v5 of Transformers.
Importing from timm.models.hub is deprecated.
Importing from timm.models.layers is deprecated.
Importing from timm.models.registry is deprecated.
torch_dtype is deprecated.
You are using a model of type llava to instantiate a model of type llava-1.5.
torch.cuda.amp.autocast is deprecated.
The generation flag top_p may be ignored.
```

### Conclusion

Run 015C successfully generated full-dataset outputs for removed-region uncertain-token masking.

## Run 015C — Full Removed-Image Evaluation with Removed-Region Masking

**Status:** Completed successfully  
**Date:** Fri Jul 3 01:51:30 CEST 2026  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run full LLaVA evaluation on all 522 ROHE removed-image samples while suppressing epistemically uncertain visual tokens only in the removed-object region.

This run tests whether hallucination is causally linked to uncertain visual tokens located in the region where the target object originally existed before object removal.

The main comparison after all region-wise runs will be:

```text
none vs all vs removed vs context vs background
```

### Input

```text
hpc_inputs/questions_removed_jpg.jsonl
hpc_inputs/removed_images_jpg/
outputs/attack_removed_full/
hpc_inputs/token_regions/
```

Input count:

```text
522 samples
```

### Configuration

```text
Model: llava-1.5-7b
Decoder: greedy
Dataset name: rohe
Number of samples: 522
Max new tokens: 64
use_ours: enabled
region_mask_mode: removed
```

### Command Meaning

The important setting for this run was:

```text
region_mask_mode = removed
```

This means only epistemically uncertain patch tokens belonging to the removed-object region were suppressed during generation.

Uncertain tokens in the context and background regions were not suppressed by this condition.

### Output

```text
outputs/eval_removed_removed/captions.jsonl
outputs/eval_removed_removed/config.json
outputs/eval_removed_removed/region_uncertainty/
```

### Output Verification

The output checks confirmed:

```text
522 outputs/eval_removed_removed/captions.jsonl
522 region_uncertainty JSON files
```

### Result

The run completed successfully:

```text
100%|██████████| 522/522 [40:44<00:00, 4.68s/it]
```

Runtime:

```text
greedy 2444.449285030365 seconds
```

Approximately:

```text
40.74 minutes
```

Final timestamp:

```text
Fri Jul 3 01:51:30 CEST 2026
```

### Masking Verification

The logs confirm that removed-region masking was active:

```text
Region mode: removed
patch_total: 576
keep_mask_total: 577
```

Several samples had non-zero removed-region suppression:

```text
Sample: sample_000571 Region mode: removed suppressed_patch: 10 patch_total: 576 keep_mask_total: 577
Sample: sample_000573 Region mode: removed suppressed_patch: 13 patch_total: 576 keep_mask_total: 577
Sample: sample_000579 Region mode: removed suppressed_patch: 15 patch_total: 576 keep_mask_total: 577
Sample: sample_000580 Region mode: removed suppressed_patch: 11 patch_total: 576 keep_mask_total: 577
Sample: sample_000599 Region mode: removed suppressed_patch: 23 patch_total: 576 keep_mask_total: 577
```

Some samples had:

```text
suppressed_patch: 0
```

This is expected. It means no uncertain patch tokens fell inside the removed-object region for those samples.

### Final Log Snippet

```text
Sample: sample_000570 Region mode: removed suppressed_patch: 0 patch_total: 576 keep_mask_total: 577
Sample: sample_000571 Region mode: removed suppressed_patch: 10 patch_total: 576 keep_mask_total: 577
Sample: sample_000572 Region mode: removed suppressed_patch: 0 patch_total: 576 keep_mask_total: 577
:
:
Sample: sample_000635 Region mode: removed suppressed_patch: 8 patch_total: 576 keep_mask_total: 577
Sample: sample_000636 Region mode: removed suppressed_patch: 0 patch_total: 576 keep_mask_total: 577
greedy 2444.449285030365
Checking outputs
outputs/eval_removed_removed/captions.jsonl
outputs/eval_removed_removed/config.json
outputs/eval_removed_removed/region_uncertainty/sample_000062.json
:
:
outputs/eval_removed_removed/region_uncertainty/sample_000094.json
outputs/eval_removed_removed/region_uncertainty/sample_000095.json
outputs/eval_removed_removed/region_uncertainty/sample_000096.json
522 outputs/eval_removed_removed/captions.jsonl
522
Status: Run 015C full eval removed none completed.
Fri Jul  3 01:51:30 CEST 2026
```

### Minor Logging Note

The final status line printed:

```text
Status: Run 015C full eval removed none completed.
```

This is only a script text-label typo inherited from the previous run script.

The actual run is correct because the output folder and per-sample logs confirm:

```text
outputs/eval_removed_removed/
Region mode: removed
```

### Raw Logs

```text
logs/run_15c_eval_removed_removed.out
logs/run_15c_eval_removed_removed.err
logs/run_15c_eval_removed_removed.condor.log
```

### Non-fatal Warnings

The `.err` log contained only non-fatal warnings:

```text
TRANSFORMERS_CACHE is deprecated and will be removed in v5 of Transformers.
Importing from timm.models.hub is deprecated.
Importing from timm.models.layers is deprecated.
Importing from timm.models.registry is deprecated.
torch_dtype is deprecated.
You are using a model of type llava to instantiate a model of type llava-1.5.
torch.cuda.amp.autocast is deprecated.
The generation flag top_p may be ignored.
```

These warnings did not stop execution.

### Conclusion

Run 015C successfully generated full-dataset outputs for removed-region uncertain-token masking.

This condition will be compared against:

```text
Run 015B: region_mask_mode = none
Run 015A: region_mask_mode = all
Run 015D: region_mask_mode = context
Run 015E: region_mask_mode = background
```

If removed-region masking reduces hallucination more than context or background masking, this would support the hypothesis that uncertain tokens in the removed-object region are causally important for object hallucination.

If removed-region masking has little effect, this may suggest that hallucination is driven more by context, background cues, or language priors.


## Run 015D — Full Removed-Image Evaluation with Context-Region Masking

**Date:** REPLACE_WITH_COMPLETION_TIMESTAMP  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run full LLaVA evaluation on all 522 ROHE removed-image samples while suppressing epistemically uncertain visual tokens only in the context region around the removed object.

This run tests whether hallucination is causally linked to uncertain visual tokens in the surrounding context rather than only the removed-object region.

The main comparison after all region-wise runs will be:

```text
none vs all vs removed vs context vs background
```

### Input

```text
hpc_inputs/questions_removed_jpg.jsonl
hpc_inputs/removed_images_jpg/
outputs/attack_removed_full/
hpc_inputs/token_regions/
```

Input count:

```text
522 samples
```

### Configuration

```text
Model: llava-1.5-7b
Decoder: greedy
Dataset name: rohe
Number of samples: 522
Max new tokens: 64
use_ours: enabled
region_mask_mode: context
```

### Command Meaning

The important setting for this run was:

```text
region_mask_mode = context
```

This means only epistemically uncertain patch tokens belonging to the context region were suppressed during generation.

Uncertain tokens in the removed-object region and background region were not suppressed by this condition.

### Output

```text
outputs/eval_removed_context/captions.jsonl
outputs/eval_removed_context/config.json
outputs/eval_removed_context/region_uncertainty/
```

### Output Verification

The output checks confirmed:

```text
522 outputs/eval_removed_context/captions.jsonl
522 region_uncertainty JSON files
```

### Result

The run completed successfully:

```text
100%|██████████| 522/522 [REPLACE_WITH_RUNTIME]
```

Runtime:

```text
REPLACE_WITH_GREEDY_SECONDS seconds
```

Approximately:

```text
REPLACE_WITH_RUNTIME_MINUTES minutes
```

Final timestamp:

```text
REPLACE_WITH_COMPLETION_TIMESTAMP
```

### Masking Verification

The logs confirm that context-region masking was active:

```text
Region mode: context
patch_total: 576
keep_mask_total: 577
```

Some samples may have:

```text
suppressed_patch: 0
```

This is expected. It means no uncertain patch tokens fell inside the context region for those samples.

Non-zero `suppressed_patch` values confirm that context-region masking was applied where uncertain context tokens existed.

### Raw Logs

```text
logs/run_15d_eval_removed_context.out
logs/run_15d_eval_removed_context.err
logs/run_15d_eval_removed_context.condor.log
```

### Non-fatal Warnings

The `.err` log contained only non-fatal warnings similar to previous runs, including:

```text
TRANSFORMERS_CACHE is deprecated and will be removed in v5 of Transformers.
Importing from timm.models.hub is deprecated.
Importing from timm.models.layers is deprecated.
Importing from timm.models.registry is deprecated.
torch_dtype is deprecated.
You are using a model of type llava to instantiate a model of type llava-1.5.
torch.cuda.amp.autocast is deprecated.
The generation flag top_p may be ignored.
```

These warnings did not stop execution.

### Conclusion

Run 015D successfully generated full-dataset outputs for context-region uncertain-token masking.

## Run 015E — Full Removed-Image Evaluation with Background-Region Masking
 
**Date:** REPLACE_WITH_15E_COMPLETION_TIMESTAMP  
**Location:** HPC GPU node  
**Dataset:** ROHE final removed-image set  
**Model:** LLaVA-1.5-7B

### Goal

Run full LLaVA evaluation on all 522 ROHE removed-image samples while suppressing epistemically uncertain visual tokens only in the background region.

This run tests whether hallucination is causally linked to uncertain visual tokens in the background rather than the removed-object region or nearby context.

The main comparison after this run is:

```text
none vs all vs removed vs context vs background
```

### Input

```text
hpc_inputs/questions_removed_jpg.jsonl
hpc_inputs/removed_images_jpg/
outputs/attack_removed_full/
hpc_inputs/token_regions/
```

Input count:

```text
522 samples
```

### Configuration

```text
Model: llava-1.5-7b
Decoder: greedy
Dataset name: rohe
Number of samples: 522
Max new tokens: 64
use_ours: enabled
region_mask_mode: background
```

### Command Meaning

The important setting for this run was:

```text
region_mask_mode = background
```

This means only epistemically uncertain patch tokens belonging to the background region were suppressed during generation.

Uncertain tokens in the removed-object region and context region were not suppressed by this condition.

### Output

```text
outputs/eval_removed_background/captions.jsonl
outputs/eval_removed_background/config.json
outputs/eval_removed_background/region_uncertainty/
```

### Output Verification

The output checks confirmed:

```text
522 outputs/eval_removed_background/captions.jsonl
522 region_uncertainty JSON files
```

### Result

The run completed successfully:

```text
100%|██████████| 522/522 [39:12<00:00, 4.51s/it]
```

Runtime:

```text
greedy 2352.300552368164 seconds
```

Approximately:

```text
39.2 minutes
```

Final timestamp:

```text
REPLACE_WITH_15E_COMPLETION_TIMESTAMP
```

### Masking Verification

The logs confirm that background-region masking was active:

```text
Region mode: background
patch_total: 576
keep_mask_total: 577
```

Several samples had substantial background-region suppression:

```text
Sample: sample_000625 Region mode: background suppressed_patch: 124 patch_total: 576 keep_mask_total: 577
Sample: sample_000626 Region mode: background suppressed_patch: 110 patch_total: 576 keep_mask_total: 577
Sample: sample_000622 Region mode: background suppressed_patch: 94 patch_total: 576 keep_mask_total: 577
Sample: sample_000617 Region mode: background suppressed_patch: 93 patch_total: 576 keep_mask_total: 577
Sample: sample_000618 Region mode: background suppressed_patch: 90 patch_total: 576 keep_mask_total: 577
```

This confirms that background-region masking was applied.

### Raw Logs

```text
logs/run_15e_eval_removed_background.out
logs/run_15e_eval_removed_background.err
logs/run_15e_eval_removed_background.condor.log
```

### Non-fatal Warnings

The `.err` log contained only non-fatal warnings similar to previous runs, including:

```text
TRANSFORMERS_CACHE is deprecated and will be removed in v5 of Transformers.
Importing from timm.models.hub is deprecated.
Importing from timm.models.layers is deprecated.
Importing from timm.models.registry is deprecated.
torch_dtype is deprecated.
You are using a model of type llava to instantiate a model of type llava-1.5.
torch.cuda.amp.autocast is deprecated.
The generation flag top_p may be ignored.
```

These warnings did not stop execution.

### Minor Logging Note

The log also contained:

```text
sort: write failed: 'standard output': Broken pipe
sort: write error
```

This did not affect the evaluation outputs.

This likely happened because the script used a command similar to:

```text
find ... | sort | head -20
```

When `head` closes the pipe after 20 lines, `sort` can report a broken pipe. The important outputs were already written and verified.

### Conclusion

Run 015E successfully generated full-dataset outputs for background-region uncertain-token masking.

This completes the core removed-image masking experiment set:

```text
Run 015A: region_mask_mode = all
Run 015B: region_mask_mode = none
Run 015C: region_mask_mode = removed
Run 015D: region_mask_mode = context
Run 015E: region_mask_mode = background
```