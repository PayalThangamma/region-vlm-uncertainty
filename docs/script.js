const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.site-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));document.querySelector('.copy-button')?.addEventListener('click',async e=>{const b=e.currentTarget;const t=document.getElementById(b.dataset.copyTarget);const old=b.textContent;try{await navigator.clipboard.writeText(t.innerText);b.textContent='Copied'}catch{b.textContent='Copy failed'}setTimeout(()=>b.textContent=old,1500)});
